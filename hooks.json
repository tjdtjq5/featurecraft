{
  "description": "FeatureCraft — Feature.md 자동 감지",
  "hooks": {
    "UserPromptSubmit": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "test -n \"$CLAUDE_PLUGIN_ROOT\" && bash \"$CLAUDE_PLUGIN_ROOT\"/scripts/detect-feature.sh || true",
            "timeout": 3
          }
        ]
      }
    ]
  }
}
